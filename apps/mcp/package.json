{
  "name": "@kanon/mcp",
  "version": "0.1.0",
  "description": "Kanon MCP server — a drop-in linear-server-compatible MCP surface over the Kanon event log (register as `linear-server`)",
  "license": "MIT",
  "type": "module",
  "bin": {
    "kanon-mcp": "./src/index.ts"
  },
  "dependencies": {
    "@kanon/core": "workspace:*",
    "@kanon/service": "workspace:*",
    "@kanon/store": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test",
    "check-types": "tsc --noEmit"
  }
}
