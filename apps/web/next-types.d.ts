// Committed ambient references for the Next app. `next-env.d.ts` is generated
// (and gitignored) because `next build` rewrites it to import build artifacts
// under .next/, which don't exist when CI runs `check-types` before `build`.
/// <reference types="next" />
/// <reference types="next/image-types/global" />
