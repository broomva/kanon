{
  "name": "@kanon/server",
  "version": "0.1.0",
  "description": "Kanon rendezvous server — REST v1, event feed, SSE, webhooks, ID allocation over the git-carried event log",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@hono/mcp": "^0.3.0",
    "@kanon/core": "workspace:*",
    "@kanon/mcp": "workspace:*",
    "@kanon/service": "workspace:*",
    "@kanon/store": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "hono": "^4.6.0"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test",
    "check-types": "tsc --noEmit"
  }
}
